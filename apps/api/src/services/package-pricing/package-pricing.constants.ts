/** Returned when an incomplete-making persist deactivates an active package (US3). */
export const PACKAGE_PRICING_INCOMPLETE_DEACTIVATE_WARNING =
  'Package was deactivated because pricing is incomplete after this change. Complete all required rates and activate explicitly.';
