name: Deploy to Fly.io

on:
  push:
    branches:
      - staging
      - production
  pull_request:
    branches:
      - staging
      - production

env:
  FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }} 

jobs:
  deploy-staging:
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    env:
      DEPLOY_INTERNAL_API: true
      AUTO_DEPLOY_DB: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test

      - name: Install Fly.io CLI
        run: |
          curl -L https://fly.io/install.sh | sh
          echo "$HOME/.fly/bin" >> $GITHUB_PATH

      - name: Deploy Internal API to Staging
        if: env.DEPLOY_INTERNAL_API != 'false'
        run: |
          flyctl deploy -a microbima-staging-internal-api -c infra/fly/internal-api/staging/fly.toml

      - name: Run Database Migrations on Staging
        if: env.DEPLOY_INTERNAL_API != 'false' && env.AUTO_DEPLOY_DB == 'true'
        run: |
          echo "🔄 Running database migrations on staging..."
          flyctl ssh console -a microbima-staging-internal-api -C "cd /app && npm exec prisma migrate deploy"
          echo "✅ Database migrations completed"

      - name: Deploy Agent Registration to Staging
        run: |
          flyctl deploy -a maishapoa-staging-agent-registration -c infra/fly/agent-registration/staging/fly.toml

      # TODO: Enable when web-admin app is created
      # - name: Deploy Web Admin to Staging
      #   run: |
      #     flyctl deploy -a microbima-staging-web-admin -c infra/fly/web-admin/staging/fly.toml

      # TODO: Enable when public-api app is created
      # - name: Deploy Public API to Staging
      #   run: |
      #     flyctl deploy -a microbima-staging-public-api -c infra/fly/public-api/staging/fly.toml

  deploy-production:
    if: github.ref == 'refs/heads/production'
    runs-on: ubuntu-latest
    environment: 
      name: production
      url: https://maishapoa-production-internal-api.fly.dev
    env:
      AUTO_DEPLOY_DB: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test

      - name: Install Fly.io CLI
        run: |
          curl -L https://fly.io/install.sh | sh
          echo "$HOME/.fly/bin" >> $GITHUB_PATH

      - name: Deploy Internal API to Production
        run: |
          flyctl deploy -a maishapoa-production-internal-api -c infra/fly/internal-api/production/fly.toml

      - name: Run Database Migrations on Production
        if: env.AUTO_DEPLOY_DB == 'true'
        run: |
          echo "🔄 Running database migrations on production..."
          flyctl ssh console -a maishapoa-production-internal-api -C "cd /app && npm exec prisma migrate deploy"
          echo "✅ Database migrations completed"

      - name: Deploy Agent Registration to Production
        run: |
          flyctl deploy -a maishapoa-production-agent-registration -c infra/fly/agent-registration/production/fly.toml

      - name: Deploy Public API to Production
        run: |
          flyctl deploy -a maishapoa-production-public-api -c infra/fly/public-api/production/fly.toml

      # TODO: Enable when web-admin app is created
      # - name: Deploy Web Admin to Production
      #   run: |
      #     flyctl deploy -a maishapoa-production-web-admin -c infra/fly/web-admin/production/fly.toml

  health-check:
    needs: [deploy-staging, deploy-production]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Health Check Staging
        if: github.ref == 'refs/heads/staging'
        run: |
          echo "🏥 Running health checks for Staging environment..."
          
          # Agent Registration App
          echo "Checking Agent Registration..."
          curl -f https://maishapoa-staging-agent-registration.fly.dev/ || exit 1
          echo "✅ Agent Registration is healthy"
          
          # Internal API (conditional check)
          if [ "$DEPLOY_INTERNAL_API" != "false" ]; then
            echo "Checking Internal API..."
            curl -f https://microbima-staging-internal-api.fly.dev/api/health || echo "⚠️ Internal API health check failed"
          else
            echo "⏭️ Skipping Internal API health check (not deployed)"
          fi
          
          # TODO: Add health checks when web-admin and public-api are deployed
          # curl -f https://microbima-staging-web-admin.fly.dev/ || exit 1
          # curl -f https://microbima-staging-public-api.fly.dev/ || exit 1
          
          echo "✅ Staging health checks completed"

      - name: Health Check Production
        if: github.ref == 'refs/heads/production'
        run: |
          echo "🏥 Running health checks for Production environment..."
          
          # Internal API
          echo "Checking Internal API..."
          curl -f https://maishapoa-production-internal-api.fly.dev/api/health || exit 1
          echo "✅ Internal API is healthy"
          
          # Agent Registration App
          echo "Checking Agent Registration..."
          curl -f https://maishapoa-production-agent-registration.fly.dev/ || exit 1
          echo "✅ Agent Registration is healthy"
          
          # Public API (Kong Gateway)
          echo "Checking Public API..."
          curl -f https://maishapoa-production-public-api.fly.dev/ || exit 1
          echo "✅ Public API is healthy"
          
          # TODO: Add health check when web-admin app is deployed
          # curl -f https://maishapoa-production-web-admin.fly.dev/ || exit 1
          
          echo "✅ All production health checks passed successfully"
