app = "maishapoa-staging-internal-api"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "staging"
  PORT = "3001"
  API_PREFIX = "api"
  # Staging-specific environment variables
  LOG_LEVEL = "debug"
  SENTRY_ENVIRONMENT = "staging"
  SENTRY_TRACES_SAMPLE_RATE = "0.1"

[[services]]
  internal_port = 3001
  protocol = "tcp"

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [services.concurrency]
    type = "connections"
    hard_limit = 25
    soft_limit = 20

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"
    # Allow time for container start + Node bootstrap before expecting :3001 open
    grace_period = "30s"

  [[services.http_checks]]
    interval = "10s"
    timeout = "5s"
    # App may spend time on config/Sentry/bootstrap; avoid false failures while still starting
    grace_period = "120s"
    method = "GET"
    path = "/health"
    protocol = "http"
    tls_skip_verify = false

[metrics]
  port = 9091
  path = "/metrics"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512

# Staging environment - less restrictive for testing
