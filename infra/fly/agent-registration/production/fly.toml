app = "maishapoa-production-agent-registration"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"
  # Build arguments for NEXT_PUBLIC_* variables (from Fly secrets)
  [build.args]
    NEXT_PUBLIC_SUPABASE_URL = "https://xmkiddtkujaparakqwem.supabase.co"
    NEXT_PUBLIC_SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inhta2lkZHRrdWphcGFyYWtxd2VtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjA1NTcwMDAsImV4cCI6MjA3NjEzMzAwMH0.p4EBZP8IRu3LBxqThW2jbDaVq3_otR742NQGTyMtJXQ"
    NEXT_PUBLIC_APP_NAME = "Maisha Poa Agent Registration"
    NEXT_PUBLIC_API_BASE_URL = "https://maishapoa-production-internal-api.fly.dev"
    NEXT_PUBLIC_INTERNAL_API_BASE_URL = "https://maishapoa-production-internal-api.fly.dev/api"
    NEXT_PUBLIC_ENABLE_BOOTSTRAP = "true"
    # Sentry configuration (client-side, embedded at build time)
    # Note: Client-side Sentry DSNs are public by design (embedded in browser code)
    NEXT_PUBLIC_SENTRY_DSN = "https://80f29418b76e462ce3af33b42e0e442b@o4509982735466496.ingest.de.sentry.io/4510119855194192"
    NEXT_PUBLIC_SENTRY_ENVIRONMENT = "production"

[env]
  NODE_ENV = "production"
  PORT = "3001"
  # Production-specific environment variables
  NEXT_PUBLIC_API_URL = "https://maishapoa-production-internal-api.fly.dev"

[[services]]
  internal_port = 3001
  protocol = "tcp"

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [services.concurrency]
    type = "connections"
    hard_limit = 100
    soft_limit = 80

  [[services.tcp_checks]]
    interval = "10s"
    timeout = "10s"
    grace_period = "30s"

[metrics]
  port = 9091
  path = "/metrics"

[[vm]]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 1024

# Production environment - optimized for performance and security
