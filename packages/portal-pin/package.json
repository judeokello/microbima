{
  "name": "@microbima/portal-pin",
  "version": "0.1.0",
  "description": "Customer portal PIN strength rules shared by API and agent-registration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
